# password-vault
The vault uses the Fernet symmetric encryption algorithm from Python’s built-in cryptography library. All stored passwords are AES-encrypted, meaning even if someone opens the file — they’ll only see gibberish.
